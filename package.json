{
  "name": "visits-project",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "currently": "^0.0.8"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
